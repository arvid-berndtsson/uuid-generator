{
  "name": "uuid-generator",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that generates UUIDs",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.16",
    "@cloudflare/workers-types": "^4.20260404.1",
    "typescript": "^6.0.2",
    "vitest": "^3.1.1",
    "wrangler": "^4.79.0"
  }
}
